import React from 'react'

const LinearBar = ({ className = "" }) => {
  return (
    <div className={`w-full h-[0.1px] shadow-lg my-7 rounded-full overflow-hidden ${className}`}>
      
    </div>
  )
}

export default LinearBar
